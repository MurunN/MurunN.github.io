# Mongolian narration output

Real MP3 narration generated by `scripts/generate_mongolian_narration.py` is written here.
The website tries these MP3 files first. If a file has not been generated yet, it falls
back to the browser's `mn-MN` speech voice when available.
